<%= form_for(@player_stat) do |f| %>
  <% if @player_stat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_stat.errors.count, "error") %> prohibited this player_stat from being saved:</h2>

      <ul>
      <% @player_stat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :stats %><br>
    <%= f.text_field :stats %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
